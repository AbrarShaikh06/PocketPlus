rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(uid) {
      return isSignedIn() && request.auth.uid == uid;
    }


    // User profile document.
    match /users/{userId} {
      allow read: if isOwner(userId);
      allow create, update: if isOwner(userId);
      allow delete: if false; // soft-delete only
    }

    // Public username → account index (doc id = lowercased username).
    // Keeps `users` private while letting signup uniqueness checks and
    // username login resolve a single document. `get` is public (needed
    // pre-auth); `list` is denied so usernames can't be enumerated. A doc
    // can only be claimed by the signed-in owner it points to, and is
    // immutable once written (so a username can't be silently reassigned).
    match /usernames/{username} {
      allow get: if true;
      allow list: if false;
      allow create: if isSignedIn()
        && request.resource.data.uid == request.auth.uid;
      allow update, delete: if false;
    }

    // Business profiles.
    match /profiles/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }

    // Owner-owned financial collections.
    match /budgets/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }
    match /transactions/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }
    match /categories/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }
    match /invoices/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }
    match /khata_customers/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }
    match /khata_entries/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }
    match /savings_goals/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }
    match /savings_entries/{id} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isOwner(request.resource.data.userId);
      allow update: if isOwner(resource.data.userId);
      allow delete: if false;
    }

    // Atomic invoice-number counters (doc id = `{userId}_{profileId}`).
    match /invoice_counters/{id} {
      allow read, write: if isSignedIn() && id.matches(request.auth.uid + '_.*');
    }

    // SMS dedup log (doc id = `{userId}_{smsHash}`). Only the SHA-256 hash is
    // stored — never raw SMS text (CLAUDE.md). Owner-only; not CA-readable.
    // Gated by doc-id prefix so the `exists()` read works even when the doc
    // is absent (no resource.data to evaluate).
    match /sms_dedup_log/{id} {
      allow read, write: if isSignedIn() && id.matches(request.auth.uid + '_.*');
    }

    // Feedback: users submit; not client-readable.
    match /feedback/{id} {
      allow read: if false;
      allow create: if isSignedIn();
      allow update, delete: if false;
    }
  }
}
